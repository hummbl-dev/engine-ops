version: '3.8'

services:
  engine-ops:
    build:
      context: ../
      dockerfile: infra/Dockerfile
    image: engine-ops:latest
    container_name: engine-ops
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
    volumes:
      - ../logs:/app/logs
    restart: unless-stopped
